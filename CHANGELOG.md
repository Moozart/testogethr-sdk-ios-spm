# Changelog

## [Unreleased]

- Updated README integration quick start to current SDK API: events are declared
  via `steps` at `initialize` (`registerSchema` removed), events are dispatched
  with `sendEvent`, and deep links are routed through `handleDeepLink`.

## [0.1.0] - 2026-05-20

- First public SwiftPM binary package release.
- Added `TestogethrSdk` binary target metadata and checksum.
